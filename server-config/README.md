# 🧭 Server IP এবং Hostname পরিবর্তনের গাইড

এই ডকুমেন্টটি Ubuntu সার্ভারে static IP এবং hostname পরিবর্তনের জন্য তৈরি করা হয়েছে। 
নিচের ধাপগুলো অনুসরণ করুন — root privilege থাকা আবশ্যক।

---

## ⚙️ Step-by-Step নির্দেশনা

### 1️⃣ Root access নেওয়া
```bash
sudo su
```

---

### 2️⃣ Netplan ফোল্ডারে প্রবেশ করা  
```bash
cd /etc/netplan
```

---

### 3️⃣ Netplan configuration ফাইল ওপেন করা  
```bash
nano <press tab>
```
এটি `80-cloud-init.yaml` অথবা `01-netcfg.yaml` জাতীয় ফাইল খুলবে।  
সেখানে IP ঠিকানা পরিবর্তন করুন (`<Change-IP>` অংশে নতুন IP দিন):

উদাহরণ:
```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: no
      addresses:
        - 192.168.1.50/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 1.1.1.1
```

---

### 4️⃣ পরিবর্তন প্রয়োগ করা  
```bash
netplan apply
```

---

### 5️⃣ Hostname পরিবর্তন করা  
```bash
hostnamectl set-hostname <name>
```

উদাহরণ:
```bash
hostnamectl set-hostname server-prod-1
```

---

### 6️⃣ /etc/hosts ফাইল আপডেট করা  
```bash
nano /etc/hosts
```
তারপর পুরনো hostname এবং IP পরিবর্তন করে নতুনটা লিখুন, যেমন:
```
127.0.0.1   localhost
192.168.1.50   server-prod-1
```

---

### 7️⃣ সার্ভার রিবুট করা  
```bash
reboot
```

---

## ✅ Verification

রিবুটের পর নিচের কমান্ডগুলো দিয়ে যাচাই করুন:
```bash
hostname
hostname -I
ip a
```

---

## 📘 Reference
- [Ubuntu Netplan Docs](https://netplan.io/examples/)
- [Hostnamectl Manual](https://man7.org/linux/man-pages/man1/hostnamectl.1.html)
